# frontend/ — WOP React + TypeScript SPA

This directory will hold the Workload Operations Platform (WOP) single-page application built
with React 18 + TypeScript + Vite.

**Status: placeholder directory only.** The SPA is scaffolded in **Task 22.1** (Vite + React +
TypeScript app, typed API client for `/v1/` endpoints, synthetic-user auth token handling).

No Node dependencies are installed and no build tooling is configured at this stage. Do not run
`npm`/`create-vite` until Task 22.1.

> Frontend hosting (Harmony Console, DEP-006) remains INTERNAL-INTEGRATION-TODO.
