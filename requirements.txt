# WOP V1 dependencies (mirrors pyproject.toml). Install with:
#   pip install -r requirements.txt
#
# Runtime (Python + AWS Lambda backend — CON-001)
boto3~=1.34
jsonschema~=4.21

# Testing and tooling (NFR-005)
pytest~=8.0
pytest-cov~=4.1
hypothesis~=6.98
moto[dynamodb]~=5.0
flake8~=7.0
black~=24.2
mypy~=1.8

# Infrastructure as code — AWS CDK (Python, CON-005). Real stacks land in Task 20.
aws-cdk-lib~=2.130
constructs~=10.3
